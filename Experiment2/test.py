import numpy as np


def main():
    # init_pos = (0, 0)
    # width = 4
    # length = 4
    # n_states = width * length
    # init_probs = np.ones(n_states)
    # if init_pos:
    #     s = init_pos[0] + width * init_pos[1]
    #     init_probs[s] = 100 * n_states
    # init_probs /= np.sum(init_probs)
    # print(init_probs)
    for i in range(1, 5):
        print(i)
    pass


if __name__ == "__main__":
    main()
